# MongoDB
spring.data.mongodb.uri=mongodb://mongo:27017/auth

# OAuth2 Redirect URIs
auth.client.redirect-uri=http://host.docker.internal:5173/callback
auth.client.silent-redirect-uri=http://host.docker.internal:5173/silent-renew

# JWT Issuer
auth.issuer.host=host.docker.internal

# CORS Configuration
cors.allowed-origins=http://host.docker.internal:5173

